# Tests — CORE-CLIENT

Stdlib-only suite with in-process fake host (`fake_host.py`, no network).
Run `python3 -m pytest tests/test_tls.py -q` for smoke, full `python3 -m pytest -q`.
See `../README.md` and `../docs/lan-testing.md` (physical LAN NOT PERFORMED).
